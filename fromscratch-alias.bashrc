#!/bin/bash

#Git aliases
alias push='git push'
alias pull='git pull'
alias commit='git commit -a'
alias commitp='git commit -a && git push'
alias pushc='git commit -a && git push'
alias add='git add'

#General aliases
alias clls='clear; ls'

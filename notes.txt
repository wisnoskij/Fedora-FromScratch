#permissive mode
sudo setenforce Permissive
#Enforcing
sudo setenforce Enforcing
#single domain
semanage permissive -a xbmc_t

#Create and enable policy
audit2allow -w -a
audit2allow -a
audit2allow -a -M mypolicy

semodule -i mypolicy.pp


#Set specific boolean
sudo setsebool -P httpd_can_sendmail 1

##Great resource
https://wiki.gentoo.org/wiki/SELinux/FAQ#How_do_I_enable_SELinux.3F

/sbin/nologin

Aliases:
push, pull, commit (remove git, and allow commit-push)
